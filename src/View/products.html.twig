<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
</head>
<body>
    <li>{% for product in products | slice(0, 3) %}
        {{product}}
    {% endfor %}</li>

    <li>{% for product in products | slice(3, 3) %}
        {{product}}
    {% endfor %}</li>

    {{ dump(products) }}
    
</body>
</html>